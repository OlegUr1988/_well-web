import TimeRangePicker from "./TimeRangePicker";

const EndTimePicker = () => {
  return (
    <TimeRangePicker
      min=""
      max=""
      isLoading={false}
      value=""
      onPick={() => {}}
    />
  );
};

export default EndTimePicker;
