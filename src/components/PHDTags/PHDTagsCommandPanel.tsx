import CommandPanel from "../CommandPanel";

const PHDTagsCommandPanel = () => {
  return (
    <CommandPanel createPath="/config/phd-tags/new" createLabel="Create" />
  );
};

export default PHDTagsCommandPanel;
